# Biming Zhou — Academic Homepage

Published at https://bimingzhou.github.io/.

The homepage was migrated directly from the existing Sites source on 5 September 2026, retaining its HTML, CSS, eight publication entries, full author lists, paper links, portrait, and CV without redesign.

## Editing the site

- `index.html`: homepage content and paper links.
- `style.css`: the existing responsive layout and styles.
- `assets/biming-zhou.jpg`: portrait.
- `assets/Biming_Zhou_CV.pdf`: CV, with view and download links.
- `.nojekyll`: serve the files directly without Jekyll processing.

The older Jekyll template files remain for reference; the active homepage is `index.html`.

Migration source: Sites version 5, source revision `3e0c7d3bcdd3c041e1ca829d2ea26cc3a4fb645d`. The migrated HTML, CSS, photo, and CV are byte-for-byte identical to that source.
